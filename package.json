{
  "name": "learner-coder",
  "private": true,
  "packageManager": "pnpm@10.30.3",
  "engines": { "node": ">=22" },
  "pnpm": { "onlyBuiltDependencies": ["esbuild"] },
  "repository": { "type": "git", "url": "git+https://github.com/freeappstore-online/learner-coder.git" },
  "scripts": {
    "dev": "pnpm --filter @learner-coder/web dev",
    "build": "pnpm --filter @learner-coder/web build",
    "preview": "pnpm --filter @learner-coder/web preview",
    "typecheck": "pnpm --filter @learner-coder/web exec tsc -b",
    "test": "pnpm --filter @learner-coder/web exec tsc -b"
  }
}
